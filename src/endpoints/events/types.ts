export type Event = {
    title: string,
    description: string,
    is_live: boolean,
    currency_id: number,
    end_date: string,
    location_address: string,
    organiser_id: number,
    sales_volume: number,
    start_date: string,
}